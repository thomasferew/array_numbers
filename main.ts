// create an empty array of numbers

let arrayLoop: number[] = []

for (let index = 0; index < 4; index++) {
    arrayLoop[index] = index *2

    game.splash(i + "multiplied tiimes two is:" + arrayLoop[i])
}
    

